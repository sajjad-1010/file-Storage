NODE_ENV=development
PORT=4000

BASE_DIR=./storage
TMP_DIR=./storage/tmp
ORIG_DIR=./storage/o
THUMB_DIR=./storage/t

MAX_UPLOAD_MB=100
ALLOWED_IMAGE_MIME=image/jpeg,image/png,image/webp
ALLOWED_VIDEO_MIME=video/mp4,video/quicktime

# JWT config:
# If Main API issues HS256 tokens with UPLOAD_TOKEN_SECRET, use:
JWT_ALG=HS256
UPLOAD_TOKEN_SECRET=hayanibaleBgonatars
# Or, for RS256, comment the lines above and provide a public key instead:
# JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nreplace-with-real-key\n-----END PUBLIC KEY-----
UPLOAD_TOKEN_TTL_SEC=300

THUMB_IMAGE_MAX=512
THUMB_VIDEO_WIDTH=512
NGINX_CACHE_MAXAGE=31536000

REDIS_URL=redis://localhost:6379

# Optional structured log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=debug
